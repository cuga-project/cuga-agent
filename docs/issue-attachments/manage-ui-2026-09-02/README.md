Evidence for manage/publish UI bugs filed from `cuga start demo_crm` (2026-09-02).

Hosted on this branch so GitHub issue bodies can embed the files. The cloud `gh --attach` upload is rejected for GitHub App installation tokens (`ghs_`).

Related issues: #741 #742 #743 #744
